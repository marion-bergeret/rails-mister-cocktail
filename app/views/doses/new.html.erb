
<%= render '/layouts/navbar' %>

<p>Add a new dose to <%= @cocktail.name  %></p>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
